# Raw QS/Scopus per-university exports — licensed data, not ours to
# redistribute via a git remote. Keep locally; do not remove this line
# without confirming the data's redistribution terms first.
data/*.xlsx

# Regenerable processed dataset — rebuild with `python -m p36.build_dataset`
data/processed/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editors
.vscode/
.idea/

# Claude Code config/subagent definitions and session-local state — local
# tooling, not project deliverable content
.claude/

# Deploy artifacts (see docs/deployment-azure.md)
deploy.zip
