"""Analysis modules, one per README "Analysis" item.

Each module's docstring lists the Core Question and Possible Questions it answers
(same numbering as README.md) and every function returns a plain pandas
object (DataFrame/Series/float) — no plotting, no report text. Findings written from
these outputs should go through .claude/agents/finding-checker.md before being
presented, especially anything claiming a relationship, a trend, or a comparison
across fields or years (see docs/methodology.md, Cross-cutting rules).
"""
